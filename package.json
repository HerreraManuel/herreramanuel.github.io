{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "portfolio",
  "author": "Manuel Herrera",
  "keywords": [
    "gatsby"
  ],
  "scripts": {
    "develop": "SET NODE_OPTIONS=--openssl-legacy-provider && gatsby develop",
    "start": "SET NODE_OPTIONS=--openssl-legacy-provider && gatsby develop",
    "build": "SET NODE_OPTIONS=--openssl-legacy-provider && gatsby build",
    "serve": "gatsby serve",
    "clean": "gatsby clean",
    "deploy": "gatsby build && gh-pages -d public -b master"
  },
  "dependencies": {
    "@material-ui/core": "^4.11.3",
    "gatsby": "^5.3.3",
    "gatsby-plugin-sass": "^3.1.0",
    "gatsby-theme-material-ui": "^1.0.13",
    "particles.js": "^2.0.0",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-particles-js": "^3.4.1",
    "react-typist": "^2.0.5",
    "sass": "^1.32.5",
    "typed.js": "^2.0.11"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0"
  }
}
